# Intentionally empty, portable repository default. Do not add
# `target-cpu=native`, x86-64-v3, AVX2, or a microarchitecture-specific ARM
# target here: Cargo source builds and downstream packagers honor this file.
# Local CPU tuning belongs in config.local.toml (see config.local.example.toml).
